import React from 'react';
import Loading from '../components/shared/Loading';

const loading = () => {
    return (
        <div>
            <Loading/>
        </div>
    );
};

export default loading;